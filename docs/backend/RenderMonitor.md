# RenderMonitor

Location: `backend/runtime/renderingmonitor.h` / `backend/runtime/renderingmonitor.cpp`

Render timing metrics for a QQuickWindow.

## Methods
- `attachWindow(window)`
- `start()`, `stop()`, `reset()`

## Properties
- `fps`, `lastFrameMs`, `frameCount`, `active`
